import { makeStyles } from "@material-ui/core";

const useState = makeStyles(theme => ({
    createSeasonRoot: {
        background: "white",
        padding: "10px",
        minHeight: "70vh",
    },

}))

export default useState;